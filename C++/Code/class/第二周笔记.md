在线编辑器地址

```
https://www.jyshare.com/compile/12/
```



## vector

正向迭代

```c++
#include <iostream>
#include <vector>
using namespace std;

int main()
{
    vector<int> myv={1,2,3};
    //定义正向迭代器it
	vector<int>::iterator it;
    //从头到尾遍历元素
	for (it=myv.begin();it!=myv.end();++it){
        printf("%d ",*it);  			
		printf("\n");
    }
	//输出：1 2 3	
   return 0;
}
```

反向迭代

```c++
#include <iostream>
#include <vector>
using namespace std;

int main()
{
    vector<int> myv={1,2,3};
	//定义反向迭代器rit
	vector<int>::reverse_iterator rit;
	//从尾到头遍历
	for (rit=myv.rbegin(); rit!=myv.rend(); ++rit){
		printf("%d ",*rit);	
		printf("\n");
	} 					 
	
   return 0;
}
```





Sort 排序

从小到大

```c++
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main()
{
    vector<int> myv={2,1,5,4,3};
    //指定递增排序
    // sort(myv.begin(),myv.end(),less<int>());
    sort(myv.begin(),myv.end());	
	//定义正向迭代器it
	vector<int>::iterator it;
    //从头到尾遍历元素
	for (it=myv.begin();it!=myv.end();++it){
        printf("%d ",*it);  			
		printf("\n");
    }
	//输出：1 2 3 4 5 	
	
   return 0;
}
```

从大到小

```c++
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main()
{
    vector<int> myv={2,1,5,4,3};
    //指定递增排序
    sort(myv.begin(),myv.end(),greater<int>());
	//定义正向迭代器it
	vector<int>::iterator it;
    //从头到尾遍历元素
	for (it=myv.begin();it!=myv.end();++it){
        printf("%d ",*it);  			
		printf("\n");
    }
	//输出：5 4 3 2 1
	
   return 0;
}
```

自定义排序

```c++
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;
struct Stud {											//学生结构体类型
	int no;													//学号
	string name;											//姓名
	Stud(int no1,string name1) {					//构造函数
		no=no1;
		name=name1;
	}
	bool operator<(const Stud &s) const {	//方式1：重载<运算符
		return no>s.no;									//用于按no递减排序
	}
};

struct Cmp {										//方式2：定义关系函数()
	bool operator()(const Stud &s,const Stud &t) const {
		return s.name<t.name; 				//用于按name递增排序
	}
};
int main() {
	Stud a[]={Stud(2,"Mary"),Stud(1,"John"),Stud(5,"Smith")};
	int n=sizeof(a)/sizeof(a[0]);
	vector<Stud> myv(a,a+n);		
									//myv: 2,Mary 1,John 5,Smith
	sort(myv.begin(),myv.end());	//默认使用<运算符, 按no递减排序
    
    for(Stud x:myv){
        printf("%d ",x.no);	
        printf("\n");
        
    }
	return 0;
}
```





```c++
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;
struct Stud {											//学生结构体类型
	int no;													//学号
	string name;											//姓名
	Stud(int no1,string name1) {					//构造函数
		no=no1;
		name=name1;
	}
	bool operator<(const Stud &s) const {	//方式1：重载<运算符
		return no>s.no;									//用于按no递减排序
	}
};

struct Cmp {										//方式2：定义关系函数()
	bool operator()(const Stud &s,const Stud &t) const {
		return s.name<t.name; 				//用于按name递增排序
	}
};
int main() {
	Stud a[]={Stud(2,"Mary"),Stud(1,"John"),Stud(5,"Smith")};
	int n=sizeof(a)/sizeof(a[0]);
	vector<Stud> myv(a,a+n);		
									//myv: 2,Mary 1,John 5,Smith
	sort(myv.begin(),myv.end());	//默认使用<运算符, 按no递减排序
    
    for(Stud x:myv){
        printf("%d ",x.no);	
        printf("\n");
        
    }
	return 0;
}
```





```c++
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;
struct Stud {											//学生结构体类型
	int no;													//学号
	string name;											//姓名
	Stud(int no1,string name1) {					//构造函数
		no=no1;
		name=name1;
	}
	bool operator<(const Stud &s) const {	//方式1：重载<运算符
		return no>s.no;									//用于按no递减排序
	}
};

struct Cmp {										//方式2：定义关系函数()
	bool operator()(const Stud &s,const Stud &t) const {
		return s.name<t.name; 				//用于按name递增排序
	}
};
int main() {
	Stud a[]={Stud(2,"Mary"),Stud(1,"John"),Stud(5,"Smith")};
	int n=sizeof(a)/sizeof(a[0]);
	vector<Stud> myv(a,a+n);		
	//使用Cmp中的()运算符, 按name递增排序
    sort(myv.begin(),myv.end(),Cmp());
    
    for(Stud x:myv){
        printf("%s",x.name.c_str());	
        printf("\n");
        
    }
	return 0;
}
```







```c++
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;
struct Stud {											//学生结构体类型
	int no;													//学号
	string name;											//姓名
	Stud(int no1,string name1) {					//构造函数
		no=no1;
		name=name1;
	}
};

int main() {
	Stud a[]={Stud(2,"Mary"),Stud(1,"John"),Stud(5,"Smith")};
	int n=sizeof(a)/sizeof(a[0]);
	vector<Stud> myv(a,a+n);		
	//使用Cmp中的()运算符, 按name递增排序
    sort(myv.begin(),myv.end(),[](Stud& a,Stud& b)->bool { return a.no<b.no; });
    
    // sort(myv.begin(),myv.end(),[](Stud& a,Stud& b)->bool { return a.name>b.name; });
    
    for(Stud x:myv){
        printf("%s",x.name.c_str());	
        printf("\n");
        
    }
	return 0;
}
```

## 1.6

```c++
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;
class Solution
{
public:
    int findKthLargest(vector<int> &nums, int k)
    {
        int n = nums.size();
        sort(nums.begin(), nums.end());
        return nums[n - k];
    }
};

int main()
{

    vector<int> myv = {2, 1, 5, 4, 3};

    Solution s;

    int answer = s.findKthLargest(myv, 5);

    printf("%d", answer);

    return 0;
}

```



**1.7**

```c++
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;

class Solution
{
public:
    int countSegments(string &s)
    {
        int n = s.length();
        if (n == 0)
        {
            return 0;
        }
        int ans = 0; // 存放答案
        int i = 0;
        while (i < n && s[i] == ' ')
        {
            i++;
        }
        // 跳过开头的空格
        int j = s.find(" ", i);
        // 查找后面的空格
        while (j != -1)
        {
            ans++; // 找到一个单词后ans增1
            i = j + 1;
            while (i < n && s[i] == ' ')
            {
                i++; // 跳过中间的空格
            }
            j = s.find(" ", i);
            // 查找下一个空格
            
            
        }
        
         if (i == n)
        {
            return ans;
            // i遍历完时返回ans
        }
        else
        {
            return ans + 1; // i未遍历完时返回ans+1
        }
       
    }
};

int main()
{

    string str = "Hello, my name is John";

    Solution s;

    int answer = s.countSegments(str);

    printf("%d", answer);

    return 0;
}
```









